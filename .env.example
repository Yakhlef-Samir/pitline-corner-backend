# Database Configuration
# For local development, use your local PostgreSQL instance
# For production, set these in your deployment platform (Render Dashboard)
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-local-password
POSTGRES_DB=pitline_corner
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://postgres:your-local-password@localhost:5432/pitline_corner

# Redis Configuration
# For local development, use local Redis or comment out
# For production, set in deployment platform
REDIS_URL=redis://localhost:6379/0

# Security - CHANGE THIS IN PRODUCTION!
SECRET_KEY=change-me-in-production-use-strong-random-key

# CORS Origins
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
SERVER_HOST=http://localhost:8000
PROJECT_NAME=Pitline Corner
VERSION=1.0.0

# Monitoring (optional - set in production deployment platform)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
